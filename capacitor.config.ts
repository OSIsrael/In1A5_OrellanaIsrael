import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'OrellanaIsrael_In1A5',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
